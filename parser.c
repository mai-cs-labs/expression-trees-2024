#include "parser.h"

#include <assert.h>

Tree parse_expression(const List* const tokens)
{
    assert(tokens != NULL);
    return Tree();
}

void print_expression(const Tree* const tree)
{
    assert(tree != NULL);
}
